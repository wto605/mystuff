bootstrap setup
beef up custom bashrc and profiles
screen/tmux
zsh

work stuff:
custom binaries in git?
spin-off work-specific files

add git for rancher (autodetect?):
	docker pull alpine/git
	add this function:
		function git () {
		    (docker run -ti --rm -v ${HOME}:/root -v $(pwd):/git alpine/git "$@")
		}

